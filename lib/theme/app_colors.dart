import 'package:flutter/material.dart';

class AppPalette {
  // Brand Colors
  static const Color primaryRed = Color(0xFFE53935);
  static const Color primaryRedDark = Color(0xFFB71C1C);

  // Backgrounds
  static const Color backgroundDark = Color(0xFF121212); // Material 2 Dark
  static const Color backgroundBlack = Color(0xFF000000); // OLED
  static const Color backgroundOcean = Color(0xFF0D1B2A); // Deep Blue

  // Surfaces
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color surfaceOcean = Color(0xFF1B263B);

  // Accents
  static const Color accentOcean = Color(0xFF415A77);
  static const Color highlightOcean = Color(0xFF778DA9);
}
