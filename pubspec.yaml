name: gru_songs
description: A high-performance music streaming app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  just_audio: ^0.9.44
  just_audio_background: ^0.0.1-beta.16
  audio_session: ^0.1.25
  http: ^1.2.1
  cached_network_image: ^3.3.1
  rxdart: ^0.27.7
  audio_video_progress_bar: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true